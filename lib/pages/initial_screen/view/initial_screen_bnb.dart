import 'package:flutter/material.dart';

class InitialScreenBnb extends StatelessWidget {
  const InitialScreenBnb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.symmetric(
        horizontal: 24,
        vertical: 20,
      ),
      child: Text('button'),
    );
  }
}
